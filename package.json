{
  "name": "Brandon's Portfolio",
  "version": "1.0.0",
  "description": "Brandon's portfolio of work and projects",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "parcel src/index.html --open",
    "build": "rm -rf dist && mkdir dist && rsync -rv * dist --exclude ./dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel-bundler": "^1.12.5",
    "sass": "^1.66.1"
  },
  "dependencies": {
    "gsap": "^3.12.2"
  }
}
